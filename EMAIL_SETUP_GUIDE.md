# 📧 คู่มือการตั้งค่าอีเมล - BARAYA PERFUME

คู่มือนี้จะแนะนำวิธีการตั้งค่าระบบส่งอีเมลยืนยันคำสั่งซื้ออัตโนมัติสำหรับเว็บไซต์ BARAYA PERFUME

## 📋 สารบัญ

1. [EmailJS Setup (แนะนำ)](#emailjs-setup)
2. [PHPMailer Setup (ทางเลือก)](#phpmailer-setup)
3. [การทดสอบระบบ](#การทดสอบระบบ)
4. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## 🌟 EmailJS Setup (แนะนำ)

EmailJS เป็นบริการส่งอีเมลที่ไม่ต้องใช้ backend server เหมาะสำหรับเว็บไซต์ Static HTML

### ข้อดี
- ✅ ไม่ต้องมี server หรือ PHP
- ✅ ตั้งค่าง่าย ใช้เวลาไม่ถึง 10 นาที
- ✅ ฟรี 200 อีเมล/เดือน
- ✅ รองรับการแนบไฟล์ (รูปสลิป)
- ✅ ปลอดภัย ไม่ต้องเก็บรหัสผ่านในโค้ด

### ขั้นตอนการตั้งค่า

#### 1. สร้างบัญชี EmailJS

1. ไปที่ [https://www.emailjs.com/](https://www.emailjs.com/)
2. คลิก **"Sign Up"** เพื่อสร้างบัญชี
3. เลือกแผน **Free** (200 emails/month)

#### 2. เชื่อมต่อบริการอีเมล

1. ไปที่ **"Email Services"** ในเมนูด้านซ้าย
2. คลิก **"Add New Service"**
3. เลือกผู้ให้บริการอีเมลของคุณ:
   - **Gmail** (แนะนำ)
   - Outlook
   - Yahoo Mail
   - หรืออื่นๆ

#### สำหรับ Gmail:

1. คลิก **"Connect Account"**
2. เข้าสู่ระบบ Gmail ของคุณ
3. อนุญาตให้ EmailJS เข้าถึง
4. ตั้งชื่อ Service (เช่น "Baraya_Gmail")
5. คลิก **"Create Service"**
6. **บันทึก Service ID** (จะเป็นรหัสแบบ `service_xxxxxxx`)

> **⚠️ สำคัญ:** ถ้าใช้ Gmail คุณอาจต้อง:
> - เปิด "Less secure app access" หรือ
> - สร้าง "App Password" สำหรับ EmailJS
> - ดูรายละเอียดเพิ่มเติม: [Gmail Setup Guide](https://www.emailjs.com/docs/user-guide/connecting-email-services/gmail/)

#### 3. สร้าง Email Template

1. ไปที่ **"Email Templates"**
2. คลิก **"Create New Template"**
3. ตั้งชื่อ Template: `order_confirmation` หรือ `template_order`
4. กำหนดเนื้อหาอีเมล:

```
Subject: 
✅ คำสั่งซื้อของคุณได้รับการยืนยันแล้ว - BARAYA PERFUME

From Name: 
BARAYA PERFUME

From Email: 
{{customer_email}}

Reply To: 
Barame07042536@gmail.com

Content (HTML):

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: 'Sarabun', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f9f9f9;
        }
        .header {
            background: linear-gradient(135deg, #d4af37, #f9d342);
            color: white;
            padding: 30px;
            text-align: center;
            border-radius: 10px 10px 0 0;
        }
        .content {
            background: white;
            padding: 30px;
            border-radius: 0 0 10px 10px;
        }
        .order-box {
            background: #f0f0f0;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            color: #666;
            font-size: 0.9em;
        }
        .button {
            display: inline-block;
            padding: 12px 30px;
            background: #06c755;
            color: white;
            text-decoration: none;
            border-radius: 25px;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>💐 BARAYA PERFUME</h1>
            <p>ขอบคุณที่สั่งซื้อสินค้ากับเรา!</p>
        </div>
        
        <div class="content">
            <h2>สวัสดีค่ะคุณ {{customer_name}} 👋</h2>
            
            <p>เราได้รับคำสั่งซื้อของคุณเรียบร้อยแล้ว!</p>
            
            <div class="order-box">
                <h3>📦 รายการสินค้า:</h3>
                <pre>{{order_list}}</pre>
                
                <h3>💰 ยอดรวมทั้งหมด:</h3>
                <p style="font-size: 1.5em; color: #d4af37; font-weight: bold;">{{order_total}} บาท</p>
            </div>
            
            <div class="order-box">
                <h3>📬 ข้อมูลการจัดส่ง:</h3>
                <p><strong>ชื่อ:</strong> {{customer_name}}</p>
                <p><strong>อีเมล:</strong> {{customer_email}}</p>
                <p><strong>ที่อยู่:</strong> {{customer_address}}</p>
                <p><strong>โทรศัพท์:</strong> {{customer_phone}}</p>
            </div>
            
            <p>✅ เราจะดำเนินการจัดส่งสินค้าของคุณโดยเร็วที่สุด</p>
            <p>📞 หากมีคำถามเพิ่มเติม สามารถติดต่อเราได้ที่:</p>
            
            <p style="text-align: center;">
                <a href="https://line.me/ti/p/OJY81U61Jk" class="button">📱 ติดต่อผ่าน LINE</a>
            </p>
            
            <div class="footer">
                <p>📧 Email: Barame07042536@gmail.com</p>
                <p>📱 LINE ID: bk0704</p>
                <p>&copy; 2025 BARAYA PERFUME - น้ำหอมแท้คุณภาพพรีเมียม</p>
            </div>
        </div>
    </div>
</body>
</html>
```

5. คลิก **"Save"**
6. **บันทึก Template ID** (จะเป็นรหัสแบบ `template_xxxxxxx`)

#### 4. ดึง User ID

1. ไปที่ **"Account"** → **"General"**
2. คัดลอก **Public Key** (หรือ User ID)
3. จะเป็นรหัสแบบ `EUHurGnUrY9Q-SbaO`

#### 5. อัปเดตโค้ดในเว็บไซต์

เปิดไฟล์ `checkout-modern.html` และแก้ไขตรงนี้:

```html
<script>
    (function(){
        emailjs.init("YOUR_PUBLIC_KEY");  // ← เปลี่ยนเป็น Public Key ของคุณ
    })();
</script>
```

และแก้ไขตรงนี้:

```javascript
emailjs.send("YOUR_SERVICE_ID", "YOUR_TEMPLATE_ID", {
    customer_name: name,
    customer_email: email,
    customer_address: address,
    customer_phone: phone,
    order_list: orderDetails,
    order_total: totalPrice,
    slip_image: e.target.result
})
```

**ตัวอย่างที่ถูกต้อง:**

```javascript
emailjs.init("YOUR_PUBLIC_KEY");  // เช่น "EUHurGnUrY9Q-SbaO"

// ในฟังก์ชัน sendOrderToEmail:
emailjs.send("YOUR_SERVICE_ID", "YOUR_TEMPLATE_ID", {  // เช่น "service_sfp9xjq", "template_order"
    customer_name: name,
    customer_email: email,
    customer_address: address,
    customer_phone: phone,
    order_list: orderDetails,
    order_total: totalPrice,
    slip_image: e.target.result
})
```

#### 6. เสร็จสิ้น! 🎉

ระบบอีเมลพร้อมใช้งานแล้ว! ทดสอบด้วยการสั่งซื้อสินค้าจริง

---

## 🔧 PHPMailer Setup (ทางเลือก)

PHPMailer เหมาะสำหรับผู้ที่มี PHP Server และต้องการควบคุมเต็มรูปแบบ

### ข้อกำหนด
- PHP 7.0 หรือสูงกว่า
- Web Server (Apache, Nginx)
- SMTP Server หรือ Gmail Account

### ขั้นตอนการตั้งค่า

#### 1. ติดตั้ง PHPMailer

PHPMailer มีอยู่ในโฟลเดอร์ `PHPMailer/` แล้ว

หากต้องการติดตั้งใหม่:

```bash
# ใช้ Composer
composer require phpmailer/phpmailer

# หรือดาวน์โหลดจาก GitHub
git clone https://github.com/PHPMailer/PHPMailer.git
```

#### 2. ตั้งค่า Gmail SMTP

1. เปิด Gmail ของคุณ
2. ไปที่ **"Manage your Google Account"** → **"Security"**
3. เปิด **"2-Step Verification"** (ถ้ายังไม่ได้เปิด)
4. ไปที่ **"App passwords"**
5. สร้าง App password สำหรับ "Mail"
6. **บันทึกรหัส 16 หลัก** ที่ได้

#### 3. อัปเดตไฟล์ send_email.php

เปิดไฟล์ `send_email.php` และแก้ไข:

```php
$mail->Username   = 'your-email@gmail.com';     // ← อีเมล Gmail ของคุณ
$mail->Password   = 'your-app-password';        // ← App Password ที่สร้างไว้
$mail->setFrom('your-email@gmail.com', 'BARAYA PERFUME');
```

#### 4. ตั้งค่า SMTP

ในไฟล์ `send_email.php`:

```php
$mail->isSMTP();
$mail->Host       = 'smtp.gmail.com';
$mail->SMTPAuth   = true;
$mail->Username   = 'your-email@gmail.com';
$mail->Password   = 'your-app-password';
$mail->SMTPSecure = PHPMailer::ENCRYPTION_STARTTLS;
$mail->Port       = 587;
$mail->CharSet    = 'UTF-8';
```

#### 5. ทดสอบ

รันไฟล์ `test_email.php` บนเซิร์ฟเวอร์:

```
http://localhost/Baraya_Perfume/test_email.php
```

---

## ✅ การทดสอบระบบ

### ทดสอบ EmailJS

1. เปิดเว็บไซต์ `index.html`
2. เพิ่มสินค้าลงตะกร้า
3. คลิก "ดำเนินการสั่งซื้อ"
4. กรอกข้อมูลทั้งหมด:
   - ชื่อ-นามสกุล
   - อีเมลจริงของคุณ
   - ที่อยู่
   - เบอร์โทร
5. อัปโหลดรูปสลิป (ใช้รูปใดก็ได้สำหรับทดสอบ)
6. คลิก "ยืนยันคำสั่งซื้อ"
7. เช็คอีเมลที่กรอกไว้

### ทดสอบ PHPMailer

1. เปิดไฟล์ `test_email.php` บนเบราว์เซอร์
2. ดูผลลัพธ์:
   - ✅ "Email sent successfully" = สำเร็จ
   - ❌ "Error: ..." = มีข้อผิดพลาด

---

## 🔍 การแก้ไขปัญหา

### EmailJS

#### ปัญหา: อีเมลไม่ถึง

**วิธีแก้:**
1. เช็คว่ากรอกข้อมูลครบหรือไม่
2. ตรวจสอบ Console ของ Browser (F12)
3. ตรวจสอบ Spam/Junk Mail
4. ตรวจสอบว่า Service ID และ Template ID ถูกต้อง
5. ตรวจสอบว่า Public Key ถูกต้อง

#### ปัญหา: Error 401 Unauthorized

**วิธีแก้:**
- Public Key ผิด หรือยังไม่ได้ init
- ไปที่ EmailJS → Account → ก็อปปี้ Public Key ใหม่

#### ปัญหา: Error 403 Forbidden

**วิธีแก้:**
- เกินโควต้า 200 อีเมล/เดือน
- อัปเกรดแพ็กเกจ หรือรอเดือนถัดไป

#### ปัญหา: รูปสลิปไม่แนบ

**วิธีแก้:**
1. ตรวจสอบว่าอัปโหลดรูปหรือยัง
2. ตรวจสอบขนาดรูป (ไม่ควรเกิน 3MB)
3. ตรวจสอบว่า Template รองรับ attachment

### PHPMailer

#### ปัญหา: SMTP Error

**วิธีแก้:**
```php
// เพิ่มการ debug
$mail->SMTPDebug = 2;
$mail->Debugoutput = 'html';
```

#### ปัญหา: Authentication failed

**วิธีแก้:**
1. ตรวจสอบ Username/Password
2. ใช้ App Password แทนรหัสผ่านจริง
3. เปิด "Less secure app access" (ไม่แนะนำ)

#### ปัญหา: Could not connect to SMTP

**วิธีแก้:**
1. ตรวจสอบ Host: `smtp.gmail.com`
2. ตรวจสอบ Port: `587` (TLS) หรือ `465` (SSL)
3. ตรวจสอบ Firewall

---

## 📊 ตารางเปรียบเทียบ

| คุณสมบัติ | EmailJS | PHPMailer |
|-----------|---------|-----------|
| ความยาก | ⭐ ง่าย | ⭐⭐⭐ ยาก |
| ต้องมี Server | ❌ ไม่ต้อง | ✅ ต้องมี PHP |
| ฟรี | 200/เดือน | ไม่จำกัด |
| รองรับ Attachment | ✅ | ✅ |
| ความปลอดภัย | ✅ สูง | ⚠️ ต้องดูแลเอง |
| Customization | ⭐⭐ ปานกลาง | ⭐⭐⭐ เยอะ |

---

## 💡 เคล็ดลับ

### EmailJS
- ใช้ฟีเจอร์ Auto-reply สำหรับตอบกลับอัตโนมัติ
- ตั้งค่า BCC เพื่อส่งสำเนาให้ตัวเอง
- ใช้ Template variables `{{variable}}` สำหรับข้อมูลไดนามิก

### PHPMailer
- ใช้ try-catch เพื่อจัดการ error
- เปิด logging สำหรับ debug
- ใช้ Queue system สำหรับอีเมลจำนวนมาก

---

## 🆘 ต้องการความช่วยเหลือ?

### EmailJS Support
- Documentation: [https://www.emailjs.com/docs/](https://www.emailjs.com/docs/)
- FAQ: [https://www.emailjs.com/docs/faq/](https://www.emailjs.com/docs/faq/)

### PHPMailer Support
- GitHub: [https://github.com/PHPMailer/PHPMailer](https://github.com/PHPMailer/PHPMailer)
- Documentation: [https://phpmailer.github.io/PHPMailer/](https://phpmailer.github.io/PHPMailer/)

### ติดต่อเจ้าของเว็บ
- LINE: [https://line.me/ti/p/OJY81U61Jk](https://line.me/ti/p/OJY81U61Jk)
- Email: Barame07042536@gmail.com

---

## ✅ Checklist การตั้งค่า

### EmailJS
- [ ] สมัครบัญชี EmailJS
- [ ] เชื่อมต่อบริการอีเมล (Gmail)
- [ ] สร้าง Email Template
- [ ] ได้ Service ID แล้ว
- [ ] ได้ Template ID แล้ว
- [ ] ได้ Public Key แล้ว
- [ ] อัปเดตโค้ดใน checkout-modern.html
- [ ] ทดสอบส่งอีเมลสำเร็จ

### PHPMailer (ถ้าใช้)
- [ ] ติดตั้ง PHPMailer
- [ ] มี PHP Server
- [ ] สร้าง Gmail App Password
- [ ] อัปเดต send_email.php
- [ ] ทดสอบด้วย test_email.php
- [ ] ทดสอบส่งอีเมลสำเร็จ

---

Made with ❤️ for BARAYA PERFUME
